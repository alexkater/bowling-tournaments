name: Deploy

on:
  push:
    branches: [main]

jobs:
  # ── Run all CI checks first ─────────────────────────────────────────
  check:
    name: All Checks
    uses: ./.github/workflows/ci.yml

  # ── Deploy web to Vercel ────────────────────────────────────────────
  deploy-web:
    name: Deploy Web (Vercel)
    needs: [check]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 9
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Build web
        run: pnpm --filter=@bowling/web build
      # ──────────────────────────────────────────────────────────────────
      # TODO: Replace with actual Vercel deploy step
      #
      # Setup:
      #   1. Install Vercel CLI or use amondnet/vercel-action
      #   2. Add secrets to GitHub repo:
      #      - VERCEL_TOKEN
      #      - VERCEL_ORG_ID
      #      - VERCEL_PROJECT_ID
      #
      # Example using Vercel CLI:
      #   - name: Deploy to Vercel
      #     run: npx vercel --prod --token=${{ secrets.VERCEL_TOKEN }}
      #
      # Example using vercel-action:
      #   - name: Deploy to Vercel
      #     uses: amondnet/vercel-action@v25
      #     with:
      #       vercel-token: ${{ secrets.VERCEL_TOKEN }}
      #       vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
      #       vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
      #       vercel-args: --prod
      # ──────────────────────────────────────────────────────────────────
      - name: Placeholder — deploy web
        run: echo "Deploy web to Vercel (configure secrets and uncomment above)"

  # ── Deploy API to Railway / Fly.io ──────────────────────────────────
  deploy-api:
    name: Deploy API (Railway)
    needs: [check]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 9
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Build api
        run: pnpm --filter=@bowling/api build
      # ──────────────────────────────────────────────────────────────────
      # TODO: Replace with actual Railway or Fly.io deploy step
      #
      # Option A — Railway (via CLI):
      #   - name: Deploy to Railway
      #     run: npx railway up --service api
      #     env:
      #       RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
      #
      # Option B — Fly.io (via flyctl):
      #   - name: Deploy to Fly.io
      #     uses: superfly/flyctl-actions@1.5
      #     with:
      #       args: deploy --config apps/api/fly.toml
      #     env:
      #       FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
      # ──────────────────────────────────────────────────────────────────
      - name: Placeholder — deploy api
        run: echo "Deploy API to Railway/Fly (configure secrets and uncomment above)"
